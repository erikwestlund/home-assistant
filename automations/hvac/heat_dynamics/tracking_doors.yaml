- id: update_door_closed_prev_hour_states
  alias: "Update Door Closed Prev Hour States"
  trigger:
    - platform: template
      value_template: >
        {{ now().minute == states('input_number.store_minute_doors') | int }}
  condition: []
  action:
#


